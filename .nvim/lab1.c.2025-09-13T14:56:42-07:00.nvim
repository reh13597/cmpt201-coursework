#define _GUN_SOURCE
#define _POSIX_C_SOURCE 200809L
#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[]) {
  char *buff = NULL;
  char size = 0;
  size_t num_char = getline(&buff, &size, stdin);
  printf("%s\n", &buff);
  free(buff);
}
