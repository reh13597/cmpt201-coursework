#include <stdio.h>
#include <sys/types.h>
#include <unistd.h>

int main(void) {
  pid_t process = fork();

  if (process == 0) {
    printf("child process");
  } else {
    printf("parent");
  }

  while (1) {
    sleep(1);
  }
}
